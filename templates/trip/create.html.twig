{% extends 'base.html.twig' %}

{% block title %}Créer une sortie{% endblock %}

{% block extrastyles %} <link rel="stylesheet" href="{{ asset('css/createTrip.css') }}"> {% endblock %}

{% block body %}
    <h3>Créer une sortie</h3>

<section>
    {{ form_start(TripForm) }}

    {{ form_widget(TripForm) }}

    {{ form_end(TripForm) }}

</section>
    <section>
        <a id="add-trip-button"  href="{{ path('place_create') }}">+</a>
        <button class="btn" type="submit">Enregistrer</button>
        <button class="btn" type="submit">Publier la sortie</button>
        <a id="cancel-trip-button" class="mobile-hide" href="{{ path('app_main') }}">Annuler</a>
    </section>

{% endblock %}
